[package]
name = "ultra-edit"
version = "0.1.0"
edition = "2024"
rust-version = "1.89"
description = "Snapshot-based, verifiable batch file editing"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
sha2 = "0.10"
uuid = { version = "1", features = ["v4"] }

[dev-dependencies]
serde_json = "1"

[lints.rust]
unsafe_code = "forbid"
